scp [-r] filename/path username@desitnation:destination place
     ^
   can be directory

--copy files over ssh--

ssh-keygen -t rsa

--generates a new ssh key--

vim.rc 

--add cool things to vim--

git -a

--add all the files that are being watched by git, as in those you've already commited
